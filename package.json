{
    "name": "akira-database-manager",
    "version": "1.0.0",
    "scripts": {
      "start": "node server/index.js",        
      "heroku-postbuild": "npm run build --prefix client",
      "build": "cd client && npm install && npm run build" 
    },
    "engines": {
      "node": "14.x" 
    },
    "dependencies": {
      "concurrently": "^6.0.0" 
    },
    "devDependencies": {}
  }
  